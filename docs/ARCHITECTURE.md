# Architecture — DentaFlow AI

Not yet written in full. Known so far: Next.js App Router + Supabase (Postgres, Auth), same
module convention as ClinicFlow AI (`lib/<module>/{constants,schema,queries,actions,import}.ts`,
`components/<module>/*`). No Supabase project is linked yet — see README.md's Getting Started.
